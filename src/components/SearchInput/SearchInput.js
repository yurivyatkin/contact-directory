import React from 'react';
// import PropTypes from 'prop-types';

const SearchInput = (props) => {
  return (
    <input
      type="text"
      placeholder="Search..."
      className="border border-solid border-gray-300 rounded p-1"
    />
  );
};

// SearchInput.propTypes = {

// };

export default SearchInput;
